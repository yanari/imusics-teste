import React from 'react';

import Card from '../../components/Card';

import { StyledContainer } from './styles';

const Home = () => {
  return (
    <StyledContainer>
      Fazer login como:
      <Card>
        Conta root
      </Card>
    </StyledContainer>
  );
};

export default Home;
